# Wuli布雷克 · 物理模拟实验室 — 前端提升建议

> 分析对象：https://wuliblake.com （首页 / 分类页 / 模拟详情页三层真实源码）
> 分析日期：2026-07-20
> 角色：前端开发工程师

## 一、现状概览

这是一个**纯静态多页站点**（vanilla HTML/CSS/JS，无框架），物理模拟用 `<canvas>` 实时绘制。整体信息架构清晰：

| 维度 | 现状 |
|------|------|
| 内容 | 6 大分类（力学 21 / 波 10 / 电磁学 8 / 热学 4 / 近代物理 4 / 天文 3），共约 50 个模拟 |
| 技术栈 | 原生 HTML + 内联 `<style>` + 原生 JS，canvas 渲染，无构建工具 |
| 主题 | 暖橙色调（`--primary-warm:#ff9800`），统一 CSS 变量 |
| 统计 | 不蒜子访问计数 + 百度统计（每页外部脚本） |
| 字体 | 模拟页引入 Google Fonts（Fredoka / Noto Sans SC） |
| 响应式 | 网格 3→2→1 列（800/480px 断点）；模拟页 1100px 断点 |

**做得好的地方（先肯定）：**
- 语义结构合理：`header` / `main` / `footer` 使用得当，链接即内容。
- 分类卡片 / 模拟卡片有 hover 上浮微交互，体验顺滑。
- 模拟页把「公式 + 画布 + 控制面板 + 知识点」分区清晰，且带 B 站视频讲解链接，教学设计用心。
- 画布做了 `resize` 监听，能随容器自适应。
- 双语文案（中 + 英）内联，覆盖国际化基础。

---

## 二、核心问题（按优先级）

### 🔴 P0 — 架构与可维护性（最大痛点）

1. **50+ 个页面各自重复整套 `<style>` 与头部/导航 HTML**
   改一次主题色要改 50 个文件。这是当前最大的技术债。
   → 抽离共享样式表 + 共享组件/布局壳，用「数据驱动」生成页面。

2. **没有全局统一导航**
   首页只有分类卡片；分类页只有「← 返回首页」；**模拟页连返回分类的入口都没有**（只有左上角 logo 回首页）。用户想从「密度」跳到「压强」必须：模拟页 → 首页 → 力学 → 压强。
   → 全站统一 sticky 顶栏（品牌 + 分类菜单 + 搜索 + 主题切换）。

3. **列表页无搜索 / 筛选 / 排序**
   力学页一次性列出 21 个模拟，找不到特定项。
   → 分类页加搜索框 + 标签筛选 + 排序；首页加全站搜索。

4. **内容是硬编码 HTML**
   新增一个模拟 = 手写一整页 + 在分类页插一张卡片。
   → 用一份「模拟数据 JSON」驱动所有页面，新增模拟只改数据。

### 🟠 P1 — 可访问性（WCAG 2.1 AA，当前明显不达标）

5. **Canvas 对屏幕阅读器不可见**
   物理可视化是网站核心价值，但没有 `role/aria-label`、没有文本替代、没有 `<canvas>` 回退内容。视障用户完全无法使用模拟。
   → `role="img"` + `aria-label` + 一个 `aria-live` 隐藏区域实时播报当前数值（如「密度 ρ = 1.000 g/cm³」）。

6. **滑块控件缺 `<label>` 关联**
   `input[type=range]` 只有相邻文字，没有 `for/id` 绑定，也没有 `aria-label`，AT 读不出「这是质量滑块」。
   → 每个滑块配 `<label for>` + `aria-valuetext`。

7. **缺少焦点可见样式与 skip-link**
   只靠浏览器默认焦点环；滑块 `appearance:none` 后未补 `:focus-visible`。无跳到主内容链接。
   → 统一 `:focus-visible` 描边；加 skip-link。

8. **未处理 `prefers-reduced-motion`**
   所有 hover 动画无条件播放，前庭功能障碍用户会不适。
   → 在 CSS 中用媒体查询关闭非必要动效。

9. **对比度与固定元素遮挡**
   footer 文字 `#a1887f` 配浅底、固定访问计数器（左下）在移动端可能与内容/footer 重叠。

### 🟡 P2 — 性能与 SEO

10. **每页外链第三方脚本**
    不蒜子 + 百度统计在每个页面加载，属阻塞/隐私顾虑。原型可默认不引入，或改用隐私友好、异步、带超时的统计。

11. **模拟页外链 Google Fonts**
    额外 DNS + 渲染阻塞。可 `font-display:swap` 或自托管，或首屏用系统字体栈。

12. **全站缺 `<meta name="description">`、Open Graph、结构化数据**
    这是明显的 SEO/社交分享短板，影响被发现与传播。

### 🟢 P3 — 视觉与体验打磨

13. **品牌与布局在三类页面不一致**
    首页头部居中大 logo；模拟页 logo 是左上角小标；分类页又是居中。统一品牌表达。

14. **无暗色模式 / 无语言切换**
    暖色亮色单一主题；双语硬编码无法切换。对学生夜间刷题、海外用户不友好。

15. **无 PWA / 离线能力**
    物理模拟是课堂/无网场景高频使用，加 `manifest` + `service worker` 可做离线可用。

16. **「更新中」死卡片每次都渲染**，且分类页无「相关模拟」「上一篇/下一篇」引导。

---

## 三、提升路线（建议落地顺序）

| 阶段 | 动作 | 收益 |
|------|------|------|
| 1 | 抽离 `styles.css` + 统一 sticky 顶栏 + 数据 JSON 驱动 | 消灭重复、统一导航、可搜索 |
| 2 | 改造模拟壳为「可访问 canvas 组件」（aria + label + live region + reduced-motion） | 达到 WCAG AA |
| 3 | 暗色模式（CSS 变量双主题）+ 移动优先响应式重构 | 体验与留存提升 |
| 4 | 补全 meta/OG/结构化数据 + 性能预算（无第三方阻塞、字体 swap） | SEO + 性能 |
| 5 | PWA 离线 + 搜索/筛选/排序 + 相关推荐 | 产品化闭环 |

---

## 四、配套交付物

已同步构建一个**响应式、数据驱动、可访问的参考原型**（`index.html` + `styles.css` + `data.js` + `app.js`），直接演示上述建议的落地方式：

- 统一 sticky 顶栏（品牌 + 分类菜单 + 全站搜索 + 暗色切换）
- 首页 hero + 分类网格；分类页搜索/筛选/排序
- 可访问的 canvas 模拟页（density 示例：aria-label + aria-live 播报 + 带 label 的滑块 + reduced-motion + 上一篇/下一篇）
- 设计系统：CSS 变量双主题，移动优先 `auto-fit` 网格
- 零外部依赖、零第三方脚本，开箱即用

详见 `index.html`（用浏览器打开即可预览）。
